; Le projet contient des conflits de peer-dependencies hérités
; (ex: ngx-ckeditor@12 demande Angular 12 alors que le projet est en Angular 13).
; legacy-peer-deps permet à `npm install` de fonctionner comme avant npm 7.
legacy-peer-deps=true
